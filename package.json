{
  "name": "random-numorstr",
  "version": "1.2.4",
  "description": "a simply lib to get a random string or number of given range",
  "main": "dist/index",
  "types": "types/index.d.ts",
  "repository": "https://github.com/isLishude/get-random.git",
  "author": "isLishude",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^8.5.1",
    "chai": "^4.1.2",
    "mocha": "^4.0.1",
    "tslint": "^5.8.0",
    "typescript": "^2.5.3"
  },
  "scripts": {
    "build": "tsc && npm run test",
    "prepublishOnly": "git push && git push --tags",
    "test": "mocha"
  }
}
